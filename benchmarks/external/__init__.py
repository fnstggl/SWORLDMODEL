"""External-benchmark harness: adapters that drive the EXACT production runtime
(`swm.world_model_v2.unified_runtime.simulate_world`) against public forecasting benchmarks,
with sealed preregistration so results are future-scoreable and tamper-evident."""
