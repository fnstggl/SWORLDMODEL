# Base requirements shared by every stage of the behaviour-ML pipeline.
# Kept small and stable. Heavier stage-specific deps live in data.txt / training.txt.
pyyaml>=6
jsonschema>=4.0
tqdm>=4.65
requests>=2.28
