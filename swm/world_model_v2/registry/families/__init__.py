"""Executable mechanism-family implementations. Each module implements one ontology group's transitions
plus their fit functions. Registration (records + packs + validation history) happens in
swm/world_model_v2/registry/build_registry.py, which writes the committed machine-readable registry."""
