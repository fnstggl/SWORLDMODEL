"""Persona = structured traits ⊕ learned latent θ_i (audit C.2).

Known entities get a strong θ_i fit to behavior history; cold/synthetic entities are sampled
from a segment distribution with WIDE, explicitly-flagged uncertainty. We do not claim to model
a named individual's inner life — only a distribution over responses given what we observe.

Stub — see docs/social-world-model-audit.md for the design. Not yet implemented."""

#: build-order and design are in docs/social-world-model-audit.md
IMPLEMENTED = False  # flip to True as this module lands; see the audit for its spec
